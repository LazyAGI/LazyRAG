# Runtime model config for ECS/internal services (static, all endpoints pre-configured).
# yaml key = AutoModel(model=...) lookup name; name field = actual model name sent to the API.

llm:
  - source: openai
    name: minimax-m27
    url: http://106.75.235.251:9000/v1/
    api_key: ${LAZYLLM_MINIMAX_API_KEY}

llm_instruct:
  - source: openai
    name: lazyllm
    url: http://host.docker.internal:8200/v1/
    skip_auth: true

reranker:
  - source: qwen3rerank
    type: rerank
    name: Qwen3-Reranker-8B
    url: http://host.docker.internal:8331/v1/rerank
    skip_auth: true

embed_main:
  - source: bgem3embed
    type: embed
    name: bgem3_emb_dense_custom
    url: http://host.docker.internal:12269/embed
    skip_auth: true

embed_sparse:
  - source: bgem3embed
    type: embed
    name: bgem3_emb_sparse_custom
    url: http://host.docker.internal:12269/sparse_embed
    skip_auth: true
