# Tests

## Structure

| Directory   | Scope                          |
|-------------|---------------------------------|
| `backend/`  | Backend tests (auth-service, core) |
| `algorithm/`| Algorithm tests (chat, parsing, processor) |

## Running Tests

- **Backend**: See `backend/README.md` for auth-service and core test commands.
- **Algorithm**: Run Python tests from `algorithm/` (chat, parsing, processor).
