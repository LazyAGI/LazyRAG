---
description: 默认使用中文注释；Python 优先使用单引号
alwaysApply: true
---

# 编码规范

## 注释

- 默认使用**中文**编写注释。
- 注释应清晰、简洁，说明代码意图，避免重复代码本身。

## Python

- 当单引号和双引号都可用时，优先使用**单引号**（`'`）。
- 示例：使用 `msg = 'hello'`，不要使用 `msg = "hello"`。

## Go

- 修改 Go 代码后，请使用 `gofmt` 格式化。